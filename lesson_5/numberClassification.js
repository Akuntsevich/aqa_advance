/**
 *      if (умова) {
 *          код що буде виконуватись якщо умова істина
 *      } else if (умова) {
 *          код що буде виконуватись якщо умова else if істина
 *      } else {
 *          код що буде виконуватись якщо умови if та else if не істина
 *      }
 */

let number = 0;

if (number > 0) {
	console.log('Число додатнє');
} else if (number < 0) {
	console.log('Число відємне');
} else {
	console.log('Чиисло дорівнює 0');
}
